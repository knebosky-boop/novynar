name: Новинар

on:
  schedule:
    - cron: "*/15 * * * *"      # кожні 15 хвилин (GitHub часто зволікає)
  workflow_dispatch:             # і кнопка «запустити зараз»

concurrency:
  group: novynar
  cancel-in-progress: false

jobs:
  obhid:
    name: Обхід каналів
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Залежності
        run: pip install -q -r requirements.txt

      - name: Згадати, на чому спинились
        uses: actions/cache/restore@v4
        with:
          path: novynar.db
          key: novynar-db-${{ github.run_id }}
          restore-keys: novynar-db-

      - name: Зміна на 13 хвилин — обхід кожні 2 хвилини
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          NOVYNAR_OWNER: ${{ secrets.NOVYNAR_OWNER }}
          NOVYNAR_READERS: ${{ secrets.NOVYNAR_READERS }}
          TZ: Europe/Kyiv
        run: python novynar.py --loop-minutes 13

      - name: Запам'ятати
        if: always()
        uses: actions/cache/save@v4
        with:
          path: novynar.db
          key: novynar-db-${{ github.run_id }}

      - name: Журнал
        if: always()
        run: tail -40 novynar.log || true
