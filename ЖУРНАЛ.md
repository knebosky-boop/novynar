# Журнал роботи над Новинарем

Хроніка рішень і граблів — щоб не переигрувати те саме заново.
Останнє оновлення: 18.08.2026, вечір. Перевірок у наборі: 225.

## Що замовлено і що вийшло

Судді набридло вручну пересилати другові новини з телеграм-каналів.
Зробили бота, який робить це сам.

**Стан: працює в бойовому режимі.** Живе на GitHub Actions, компʼютер не
потрібен. Отримувачі: власниця (зі звуком) і Микола (тихо, id 129576564).

## Ключові розвилки і чому обрали так

**Юзербот чи бот?** Спершу написали на Telethon (читання під акаунтом судді) —
`novynar_account.py`, лишився про запас. Потім зʼясувалось, що всі 11 каналів
відкриті, тож перейшли на читання публічних сторінок `t.me/s/<канал>`.
Виграш: акаунт судді взагалі не задіяний, ні пароля, ні ключів, ні ризику
блокування. Обмеження: закриті канали так не прочитати.

**Де розмістити.** Oracle Cloud відмовив у реєстрації (типово для України).
Дрібні «безкоштовні хостинги ботів» відкинули — їм довелося б віддати токен,
і в них «24/7» з продовженням щодоби. Обрали GitHub Actions: публічний
репозиторій = безлімітні хвилини.

**Розклад GitHub не працює.** За півтори години не спрацював жодного разу.
Рішення: одна зміна працює 18 хв (обхід кожні 2 хв), останнім кроком будить
наступну через `gh workflow run` (потрібен PAT, бо GITHUB_TOKEN не має права
тригерити workflow). Cron раз на 3 години лишили як рятівний будильник,
плюс крок-сторож глушить зміну, якщо вже працює інша.

## Граблі, кожні з яких коштували часу

1. **Telegram не забирає файли з cdn телеграму за URL** — «failed to get HTTP
   URL content». Качаємо самі, шлемо multipart, далі за `file_id`.
2. **Розмітка**: лише b/i/u/s/code/pre/a, у `<a>` тільки href. t.me підставляє
   в посилання `onclick` — через нього повідомлення відбивалося цілком.
3. **Розріз довгого тексту** ламав теги → «Unexpected end tag». Тепер на місці
   розрізу теги закриваються й відкриваються знову в наступній частині.
4. **Позиція розрізу** бралася з уже обробленого тексту — хвіст зʼїжджав.
5. **Пошук стоп-слів простим `in`**: «ставк» різало «відставку» й «поставки»,
   «партнерськ» — «контакти з партнерами», «гази» ховалося в «магазині».
   Найгірше: **«у напрямку» зʼїдало ВСІ фронтові зведення** («на Покровському
   напрямку»). Тепер пошук з межі слова.
6. **Втрачені команди**: при порожньому offset бот пропускав «усі старі»
   команди — так загубилася перша заявка Миколи. Тепер відсіюються лише
   реально старі (за датою повідомлення).
7. **Дві копії бота** (локальна + серверна) конфліктують за getUpdates.
8. **Схема бази живе в кеші** й не перестворюється при оновленні коду —
   потрібна міграція (ALTER TABLE ADD COLUMN).
9. **Відео йшло обкладинкою.** На сторінці каналу є пряме посилання на mp4 —
   тепер шлемо саме відео (ліміт Bot API 45 МБ). Гіфки — через sendAnimation.
   Перший читач вантажить файл, далі шлемо за `file_id`.
10. **Відео, друга серія.** Мало навчити слати відео — виявилось, що частина
   постів із відео взагалі не розпізнавалась як відео: у списку каналу Telegram
   часом віддає лише обкладинку, без тега `<video>`, і такий пост ішов як
   звичайне фото. Тепер відео визначаємо і за плеєром, і за обкладинкою.
   А ще Telegram сам мітить частину роликів класом `not_supported` — їх він
   не віддає вебу **взагалі**, ні в списку, ні на сторінці поста
   (`t.me/канал/id?embed=1`). Таких приблизно чверть. Для них шлемо обкладинку
   з чесною позначкою «🎬 відео — за посиланням нижче», а сторінку поста
   даремно не смикаємо. Реально файлом іде 26 із 35 роликів (74%).
11. **Гігантський пост рвав ліміт.** При досягненні 8-ї частини залишок
   запихався цілком — понад 4096 знаків, і Telegram відбивав повідомлення,
   тобто новина гинула повністю. Тепер остання частина обрізається, а в кінці
   стоїть «…решта за посиланням». Перевірено на 33 і 110 тис. знаків.
12. **Зведення зі згорнутими посиланнями не сподобалось.** Разову вибірку за
   день спершу надіслав компактним списком «час · канал · 330 знаків ·
   ↗ читати» — судді таке не годиться. Правильний формат — кожна новина
   окремим повідомленням з повним текстом і медіа, як у щоденній роботі.
   Зведення лишається тільки для нічної черги.

13. **Стоп-слова не ловлять прохання грошей.** Смолій написав «Підтримайте
   і мене та мій канал… Monobank: 4441111401673367» — між «підтримайте» і
   «канал» стояли слова, а банк латиницею, тож підстрока не збіглася. У Левіна
   пролізло «дякують підписникам каналу за допомогу у придбанні автомобіля».
   Тепер є `MONEY_PATTERNS` — візерунки: номер картки, банка, PayPal, IBAN,
   «підтримати» поруч зі словом «канал/мене/автора», подяка за збір. Подяку
   рахуємо лише поруч із передплатниками чи збором, інакше «Зеленський
   подякував партнерам за підтримку» перестало б бути новиною.
14. **Голі назви банків різали новини.** Стоп-слова «монобанк», «приватбанк»,
   «реквізити», «на карту» вбивали «Монобанк запустив послугу» і «виплати
   надійдуть на карту». Прибрано — прохання ловить візерунок із номером.
15. **Величезне посилання вбивало новину.** Розріз не можна ставити всередині
   тега; якщо href довший за ліміт, розріз упирався в початок тега й **не
   зсувався ані на знак**. Цикл нарубав сім порожніх повідомлень («2 з 8»,
   «3 з 8»…) і восьме з «…решта за посиланням» — новина гинула повністю. Саме
   це й бачила суддя в каналі «Тарас Григорович повідомляє». Тепер `cut_pos`
   ніколи не повертає нуль: перескакує тег і добирає за ним тексту, щоб
   частина не вийшла порожньою. Гігантський тег не переноситься в наступну
   частину (інакше він повторювався у кожній), а його осиротілий закривач
   вичищається — на непарному `</a>` Telegram відбиває все повідомлення.

16. **Канал виправив пост, а в читача лишилась стара версія.** Саме це
   впіймала суддя на «Тарасі Григоровичу»: новина пішла, канал її переписав,
   у нас — неправлена. Причина: обхід дивився тільки на пости, новіші за
   `last_id`, а вже надіслані більше не звіряв. Тепер у базі є `sent` і
   `sent_msg` — відбиток тексту й номери повідомлень, якими новину віддали
   кожному читачеві. Кожного обходу текст звіряється з тим, що зараз на
   сторінці; змінився — правимо на місці (`editMessageText` /
   `editMessageCaption`).

   **Де тут пастки.** По-перше, відбиток береться з тексту **без розмітки**:
   Telegram час від часу віддає ті самі слова іншими тегами, і по «сирому»
   HTML кожен обхід виглядав би як виправлення. По-друге, поріг «суттєвості»
   рахується **по знаках**, а не по словах: у короткій новині одне слово — це
   шоста частина тексту, тож за словами одрук і «протиправним → правомірним»
   виглядають однаково. По-третє, правити на місці можна лише коли частин
   рівно стільки ж, скільки надіслали, — інакше хвіст новини лишиться від
   старої версії; у такому разі виправлена новина йде окремим повідомленням.
   Стежимо 24 години й лише поки пост видно на сторінці каналу: заміряно —
   від 1,1 год у Смолія до кількох діб у спокійних каналів.

17. **Новини приходили обрізаними — і це був не розріз.** Суддя показала
   «Аналітику фронту»: пост на 4134 знаки прийшов уривком на 186. Виявилось,
   що ми взагалі брали не той текст. Коли пост є **відповіддю на інший**,
   Telegram кладе в розмітку два блоки `tgme_widget_message_text`: першим —
   урізану цитату того допису (`js-message_reply_text`, обривається на «…»),
   і лише другим — власний текст поста (`js-message_text`). Селектор брав
   перший-ліпший, тож у читача виявлялась цитата чужого поста замість новини.
   Тепер беремо саме `js-message_text`, а розмітку без js-класу — як раніше.

   Масштаб: на зрізі з 202 постів дванадцяти каналів відповідями є **30 (15%)**,
   у «Аналітики фронту» — 12 із 20. Тобто кожна сьома новина місяцями йшла
   уривком, а фільтри й дедуп працювали по чужому тексту.

## Як влаштований відсів

Порядок: службові позначки → чужа тема каналу → сигналізація → стоп-слова →
ключові слова → довжина. Далі — дедуп.

**Сигналізація проти новини.** Тверді ознаки («дорозвідка», «курсом на»,
«тривога») ріжуть завжди. Мʼякі («шахед», «вибух», «працює ППО») — лише в
коротких сигналках до 200 знаків, і **скасовуються**, якщо є ознаки підсумку
(«збили», «внаслідок», «повідомляє»). Тому «Нові шахеди зайшли на Чернігівщину»
ріжеться, а «Сили оборони збили 42 з 48 шахедів минулої ночі» проходить.

**Дедуп — три сходинки**: збіг слів 0.55; збіг 0.50 плюс 3 спільні опори;
збіг 0.35 плюс 5 опор. Опори — власні назви, абревіатури (ВПК, ДБР) і дати.
Просто знижувати поріг не можна: на 0.44 «відкрили міст» і «закрили міст на
ремонт» стають «дублем». На архіві з 1739 постів це дає 61 склейку між
каналами, з них помилкова одна.

**Окремі правила каналів**: у Смолія беремо лише тексти від 220 знаків (решта
в нього — оперативка), у Левіна не беремо суто ізраїльські новини (якщо поруч
згадані Україна чи РФ — пост проходить).

## Що вміє бот

Команди читача: `/start`, `/stop`, `/status`, `/digest`, `/help`.
Команди власниці: `/add @канал`, `/del @канал`, `/allow`, `/deny`,
`/pause`, `/resume`, `/лист <текст>` — передати Миколі особисте слово.

Доставка: власниці зі звуком, читачам тихо. Нагадування о 08:00 («Доброго
ранку! Найкращого тобі дня…») і о 20:00 («Гарного вечора тобі, Миколко!») —
зі звуком, і лише якщо є що читати. Вночі 23:00–08:00 тиша, вранці — зведення.

## Що додалось наприкінці дня

- **Нагадування**: 08:00 «Доброго ранку! Найкращого тобі дня…» і 20:00
  «Гарного вечора тобі, Миколко!» — зі звуком, лише якщо є що читати.
  Імена в `NAMES`, кличний відмінок у `NAMES_VOCATIVE` (інакше вийде
  «Гарного вечора, Микола»).
- **`/лист <текст>`** — власниця пише Миколі особисте просто з телеграму,
  бот передає зі звуком. Читачам ця команда недоступна.
- **Звук роздільний**: власниці зі звуком (щоб бачила, що бот живий),
  читачам тихо.
- **Разова вибірка за день**: 12 каналів, 86 постів у Смолія → 12 після
  фільтрів; разом 79 → 67 після склеювання повторів. Надіслано обом,
  кожна новина окремо, збоїв нема.
- **Швидкодія**: обхід 12 каналів — 9,4 с при циклі 2 хвилини (запас 92%).
  Каналів можна додати вп'ятеро більше.

## Перевірено на збоях

Обрив мережі, ліміт 429 (чекає й повторює), недоступний t.me, мертвий канал у
списку, порожній пост, самі емодзі, самі пробіли, нульовий байт у тексті,
незакрита й вивернута розмітка — ніщо не роняє бота й не зупиняє обхід решти.

## Незакриті питання

- Ссилка-перепустка закрита (`INVITE_CODE = ""`). Щоб підключити ще когось —
  вписати код і дати посилання `?start=КОД`.
- Друге вікно Claude робить бота для мами судді — пошук відповідей на
  бухгалтерські питання (`~/.claude/Бухгалтер/`). Йому передано наш скелет і
  список граблів. Там складніше: новину досить переслати, а на бухгалтерське
  питання треба знайти відповідь і не збрехати в ній.

## Перед будь-якою правкою

`.venv/bin/python perevirka.py` — 225 перевірок, нічого нікому не надсилає.
Ганяти після кожної зміни фільтрів: половину написано вже після того, як
тести знайшли справжні баги.

## 19.08 — побажання замість новини

Левін кинув пост «Доброй ночи.» — і він **пройшов** усі фільтри. Розбір: у пості
65 знаків, бо після двох слів іде підпис `@yigal_levin` — саме підпис добирає
довжину до `MIN_LENGTH = 40`. А оскільки пост із картинкою, поріг довжини для
нього взагалі не застосовується.

Тому окремий фільтр `is_greeting()`: знімаємо розмітку, посилання й підписи
`@канал`, і якщо від поста лишилось **не більше 60 знаків** і в них є візерунок
побажання (`GREETING_PATTERNS` — ніч, ранок, вечір, день, вихідні, сни; укр. і рос.) —
ріжемо з причиною «побажання («доброй ночи»)».

Поріг довжини тут головний, а не список слів: новина «Зеленський побажав доброї
ночі захисникам» і пост «Доброго ранку. Сьогодні росіяни атакували Одесу…»
довші за 60 знаків і проходять. Без порога фільтр з'їдав би такі новини.

Фільтр загальний, не лише для Левіна: «Доброго ранку» кидає не він один,
а поріг робить це безпечним.

Перевірка: 238 правильно, 0 помилок (було 225 — додано 13 перевірок).

**Знайдено, але не полагоджено:** факт надсилання новини **не пишеться в журнал**.
`broadcast()` і `enqueue()` викликаються без `log.info` (`novynar.py`, гілка
`fresh`), а рядок «надіслано» живе лише у `release_holding()`, тобто при
`HOLD_MINUTES > 0`. Наслідок: у логах GitHub Actions за добу нуль підтверджень
доставки, хоча новини читачам приходили (підтвердила суддя). Видно тільки
відсіви. Лікується одним рядком у двох місцях.

**Дрібне:** GitHub попереджає, що `actions/checkout@v4`, `setup-python@v5`,
`cache@v4` тримаються на Node 20, який виводять з обігу.

## 19.08 — щоденне зведення ОВА і російська реклама перевезень

Доброполье щодня переказує зведення ОВА: «За 18 серпня росіяни вбили 1 жителя
Донеччини: у Дружківці. Ще 4 людини в області за добу дістали поранення».
Це щоденна статистика, а не новина — суддя попросила прибрати.

Пастка тут одна й серйозна: **не можна різати за словом «поранено»**. У тому ж
каналі є «Трьох цивільних поранено, серед них 11-річний хлопчик: наслідки
бомбардування Краматорська» — це новина. Тому `is_daily_toll()` ловить не слово,
а **зв'язку підрахунку з періодом**: «за добу … дістали поранення» або
«за <число> <місяць> росіяни вбили». Новина про конкретний обстріл періоду
не має і проходить.

Заодно: реклама перевезень від Доброполья йшла **російською** («ГРУЗОВЫЕ
ГРУЗОПАССАЖИРСКИЕ ПЕРЕВОЗКИ, телефони»), а в стоп-словах були лише українські
варіанти. Додано `пассажирские перевозки`, `грузопассажирские`,
`грузовые перевозки`, `перевозки пассажиров`, `едем из`, `едем до`.

**Прогін по всіх 12 живих каналах: 204 пости, новими фільтрами зрізано рівно 2** —
зведення ОВА і «Доброй ночи» Левіна. Хибних спрацювань немає.

Перевірка: 248 правильно, 0 помилок (було 238).

## 19.08 — «Суспільне Новини» прибрано зі списку каналів

Суддя сказала, що новини цього каналу їй не потрібні. `suspilne_news` вилучено
з `SOURCES` у `config.py` — каналів лишилось 11, усі читаються.

**Увага:** `config.SOURCES` — це лише **стартовий** список; `bootstrap_sources()`
доливає з нього нові рядки в таблицю `sources`, але нічого звідти не викидає.
Тому в живій базі (кеш GitHub Actions) канал лишається `active = 1`, доки
власниця не надішле боту `/del @suspilne_news`. Правка config захищає лише від
повернення каналу після чистки кешу.

Перевірка: 248 правильно, 0 помилок. Живих каналів 11 з 11.

## 20.08 — приписка до чужого збору і скасування нічної тиші

**Прорвалось.** Левін кинув збір на 93-тю бригаду двома постами: перший — сам
збір із монобанком і номером картки (його `MONEY_PATTERNS` зрізали як завжди),
другий — коротка приписка від автора: «Друзья, мы этим бойцам уже помогали,
и не раз. Тут небольшой сбор — давайте быстро закроем и его». У ній **немає
жодного реквізиту** — ні банки, ні картки, ні слова «підтримайте канал», —
тому вона пройшла всі фільтри.

Ловимо тепер зв'язку «збір» + заклик закрити його **в першій особі множини**:
`сбор … закроем`, `закриваємо … збір` і навпаки. Особа тут головна: «Збір
підписів **закрито**» і «Триває **збір урожаю**» — новини, і вони проходять,
бо це не заклик. Окремо додано голі заклики скинутись: `скинемось`, `скиньмо`,
`докиньте`, `докинемо` — вони про гроші завжди, без жодного контексту.

Прогін по всіх 11 живих каналах: 184 пости, новими візерунками зрізано **рівно
один** — той самий пост Левіна. Хибних спрацювань нема.
Перевірка: 257 правильно, 0 помилок (було 248 — додано 9 перевірок).

**Нічної тиші більше немає.** `QUIET_HOURS = None`: раніше з 23:00 до 08:00
новини складались у чергу й вивалювались уранці, тепер ідуть цілодобово, як
удень. Нагадування о 08:00 і 20:00 лишились без змін (`REMINDER_TIMES`).

⚠ Звук: у власниці `SILENT_OWNER = False`, тобто **нічні новини прийдуть їй зі
звуком**. Якщо заважатиме — або `SILENT_OWNER = True` назавжди, або дописати
окреме нічне «тихо» (звуку немає з 23:00 до 08:00, решту доби зі звуком).
Читачам як було, тихо цілодобово.

## 20.08 — факт надсилання нарешті пишеться в журнал

Баг, знайдений 19.08: у логах GitHub Actions за добу було видно **самі відсіви**
й жодного підтвердження доставки, хоча новини читачам приходили. Причина:
`log.info` про надсилання жив тільки в `release_holding()`, тобто спрацьовував
лише при `HOLD_MINUTES > 0`, а звичайна гілка `fresh` викликала `broadcast()`
і `enqueue()` мовчки.

Полагоджено не в місцях виклику, а **всередині самих функцій** — тоді жоден
новий виклик не забуде записати:
- `broadcast()` → `надіслано (2 чит.): tgp_news — https://t.me/tgp_news/77`
- `enqueue()` → `у чергу: <канал> — <посилання>`

Дублікати в `release_ready()` прибрано, бо тепер пишуть самі функції.
Перевірка: 260 правильно, 0 помилок (додано 3 — журнал перехоплюється
`logging.Handler` і звіряється дослівно).

## 20.08 — повний прогін «усе, що можна»

Перевірка на живих даних, нікому нічого не надіслано (Bot API заглушено):

- **Перевірка**: 260 правильно, 0 помилок. Усі файли компілюються.
- **Обхід 11 каналів**: 184 пости → 135 новин, 49 зрізано (27%). Причини:
  закоротке для каналу 19, збір грошей 14, стоп-слово 5, оперативка 4,
  закоротке 4, щоденна статистика 1, службова позначка 1, побажання 1.
- **Смолій — 20 постів, 0 новин.** Це не поломка: канал зараз гонить саме
  оперативку («Ще ракета», «Київщина - чисто»), а поріг 220 знаків для нього
  на те й ставили. Єдиний довгий пост каналу — прохання грошей.
- **Повний цикл доставки** на окремій базі: відкотили позиції на 5 постів —
  зібрано 22 новини, розіслано 80 повідомлень, порівну обом читачам, черга
  порожня. Другий обхід одразу — тиша, повторів немає.
- **Звук** звірено на СПРАВЖНІЙ `api()` (підмінено лише `requests.post`):
  судді `disable_notification = False`, Миколі `True`. Нагадування о 08:01
  і 20:02 йдуть, о 03:00 тиша — нічні новини читача не будять.
- **Бот живий**: `getMe` відповідає, `@Kate_news_2026_bot`. Секрети на місці.

**Живе підтвердження нового журналу**: у зміні 32333565286 (перша з новим
кодом) о 08:11:59 з'явився рядок
`надіслано (2 чит.): babel — https://t.me/babel/88385`. До сьогодні таких
рядків у логах Actions не було жодного — тільки відсіви.

README поправлено: там ще стояла нічна пауза до ранку.
